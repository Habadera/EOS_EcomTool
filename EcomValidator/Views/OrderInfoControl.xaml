<UserControl x:Class="EcomValidator.Views.OrderInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Label Content="Environment:"/>
                <ComboBox x:Name="OrderEnvCombo" Width="150" Padding="6" SelectionChanged="OrderEnvCombo_SelectionChanged">
                    <ComboBoxItem Content="Prod" IsSelected="True"/>
                    <ComboBoxItem Content="Custom"/>
                </ComboBox>
                <TextBox x:Name="OrderCustomBaseUrlBox" Visibility="Collapsed" Margin="0,5,0,0" Width="150" ToolTip="Custom Base URL" Style="{DynamicResource FormTextBox}"/>
            </StackPanel>

            <DockPanel Grid.Column="1">
                <Label Content="Order IDs (One per line, comma or space):" DockPanel.Dock="Top"/>
                <TextBox x:Name="OrderIdBox" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto"
                         Style="{DynamicResource FormTextBox}"
                         Height="60"/>
            </DockPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0,0,0">
                <Button x:Name="OrderFetchBtn" Content="Fetch Orders" Click="OrderFetchBtn_Click" FontWeight="Bold"/>
                <Button x:Name="CopyOrderJsonBtn" Content="Copy JSON" Click="CopyOrderJsonBtn_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid x:Name="OrderHeaderGrid" 
                  Grid.Row="1" 
                  RowDetailsVisibilityMode="Collapsed"
                  SelectionMode="Single"
                  BorderBrush="{DynamicResource Brush.Border}"
                  BorderThickness="1"
                  ScrollViewer.CanContentScroll="False">

            <DataGrid.Columns>
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton Click="RowExpander_Click"
                                          IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}, Mode=OneWay}"
                                          Width="24" Height="24"
                                          Background="Transparent" Foreground="{DynamicResource Brush.FG}" BorderThickness="0">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Content" Value="+"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DetailsVisibility, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="Visible">
                                                <Setter Property="Content" Value="-"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="IsChecked" Value="True"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="*" MinWidth="180"/>
                <DataGridTextColumn Header="Status" Binding="{Binding OrderStatus}" Width="120"/>
                <DataGridTextColumn Header="Date" Binding="{Binding CreationDate}" Width="180"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice}" Width="100"/>
                <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="80"/>
            </DataGrid.Columns>

            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Background="#1A1A1A" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,1,0,1" Padding="20">
                        <StackPanel HorizontalAlignment="Stretch">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="ORDER DETAILS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>
                                    <TextBlock Text="Order ID" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding OrderId}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Invoice ID" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding InvoiceId}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Parent Order ID" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding ParentOrderId}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Order Type" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding OrderType}" Style="{DynamicResource DetailValue}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="FINANCIALS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>
                                    <TextBlock Text="Total Price" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Style="{DynamicResource DetailValue}">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0} {1}">
                                                <Binding Path="Symbol"/>
                                                <Binding Path="TotalPrice"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                    <TextBlock Text="Total Tax" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding TotalTax}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Total Discounted" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding TotalDiscounted}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Currency" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding Currency}" Style="{DynamicResource DetailValue}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="TIMESTAMPS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>
                                    <TextBlock Text="Creation Date" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding CreationDate}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Completed At" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding CompletedAt}" Style="{DynamicResource DetailValue}"/>
                                    <TextBlock Text="Last Modified" Style="{DynamicResource DetailLabel}"/>
                                    <TextBlock Text="{Binding LastModifiedDate}" Style="{DynamicResource DetailValue}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="STATUS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>
                                    <TextBlock Text="Order Status" Style="{DynamicResource DetailLabel}"/>
                                    <Border Background="#333" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,10">
                                        <TextBlock Text="{Binding OrderStatus}" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <Separator Background="{DynamicResource Brush.Border}" Margin="0,15"/>

                            <TextBlock Text="LINE OFFERS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>

                            <ItemsControl ItemsSource="{Binding LineOffers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#333" BorderThickness="0,0,0,1" Margin="0,0,0,10" Padding="0,0,0,10">
                                            <StackPanel>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="2*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="1.5*"/>
                                                        <ColumnDefinition Width="1.5*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                                        <TextBlock Text="Offer Title" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding Title}" Style="{DynamicResource DetailValue}" Foreground="White" TextWrapping="Wrap"/>
                                                        <TextBlock Text="Offer ID" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding OfferId}" Style="{DynamicResource DetailValue}" FontFamily="Consolas"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                                        <TextBlock Text="Quantity" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding Quantity}" Style="{DynamicResource DetailValue}"/>
                                                        <TextBlock Text="Unit Price" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding UnitPrice}" Style="{DynamicResource DetailValue}"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                                        <TextBlock Text="Total Price" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding TotalPrice}" Style="{DynamicResource DetailValue}"/>
                                                        <TextBlock Text="Discounted Price" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding DiscountedPrice}" Style="{DynamicResource DetailValue}"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                                                        <TextBlock Text="Seller Name" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding SellerName}" Style="{DynamicResource DetailValue}"/>
                                                        <TextBlock Text="Seller ID" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding SellerId}" Style="{DynamicResource DetailValue}" FontFamily="Consolas"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="4">
                                                        <TextBlock Text="Namespace" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding Namespace}" Style="{DynamicResource DetailValue}" FontFamily="Consolas"/>
                                                        <TextBlock Text="Display Name" Style="{DynamicResource DetailLabel}"/>
                                                        <TextBlock Text="{Binding NamespaceDisplayName}" Style="{DynamicResource DetailValue}"/>
                                                    </StackPanel>
                                                </Grid>

                                                <StackPanel Margin="0,10,0,0">
                                                    <TextBlock Text="OFFER ITEMS" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>
                                                    <DataGrid ItemsSource="{Binding OfferItems}" AutoGenerateColumns="False" Background="#222" BorderBrush="#444" BorderThickness="1" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333" Margin="0,0,0,0">
                                                        <DataGrid.Columns>
                                                            <DataGridTextColumn Header="Item ID" Binding="{Binding ItemId}" Width="250" FontFamily="Consolas"/>
                                                            <DataGridTextColumn Header="Entitlement ID" Binding="{Binding EntitlementId}" Width="250" FontFamily="Consolas"/>
                                                            <DataGridTextColumn Header="Status" Binding="{Binding EntitlementStatus}" Width="100"/>
                                                            <DataGridTextColumn Header="Redeemed?" Binding="{Binding EntitlementRedeemed}" Width="100"/>
                                                            <DataGridTextColumn Header="Redeemed At" Binding="{Binding EntitlementRedeemedAt}" Width="150"/>
                                                        </DataGrid.Columns>
                                                    </DataGrid>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
    </Grid>
</UserControl>